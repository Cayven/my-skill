---
name: outfit-point-line-network
description: 穿搭单品深度解析与审美积累方法论。以「点线网」为底层思考框架，按五部分结构拆解每件衣服：历史溯源、时尚史脉络、色彩分析、制作与面料工艺、地区文化溯源。每次分析同时产出完整深度解析 + 七维（风格定调/色彩构析/形制匠艺/本源溯往/衣脉沿革/风尚流变/地域文脉）关键词复制框HTML页面（含品名）。当用户上传衣服图片、要求拆解一件衣服、分析单品、提取穿搭关键词、建立穿搭知识体系、或寻求有文化依据的搭配参考时使用。
---

# 穿搭单品深度解析 · 点线网方法论

以「点线网」为底层思考方法，按固定五部分结构输出每件单品的深度解析。点 = 单品携带的全部信息，线 = 信息之间的演变关联，网 = 跨风格/品牌/文化的关系网络。思考时用点线网，输出时用五部分结构。

## 工作流程

### 后台步骤1：图片预处理（自动执行，不展示过程）

用户上传图片后，先在后台对图片进行标准化处理，**不在输出中展示过程**。处理脚本：`scripts/preprocess_image.py`。

**两张图片工作流程（推荐，用户通常发两张图）：**
当用户上传两张图片时，按以下分工处理：
- **图一（第一张）**：商品详情页截图（包含标题、价格、销量等信息）。**用途**：从中提取品牌名和完整产品标题。不需要预处理图片，只用于文字识别。
- **图二（第二张）**：商品图片浏览模式（黑色背景，中间是白底商品主图，通常显示"x/10"页码）。**用途**：从中裁剪出干净的白底商品图，预处理成正方形居中。这张图的商品图更干净，没有分享、购物车等UI图标。

**图二预处理步骤：**
1. 使用 `scripts/extract_white_panel.py` 检测大面积近白商品图区域；默认顶部额外裁掉约80像素。检测失败时不要猜测性裁剪，并继续用原图人工判断。
2. 将提取后的商品图交给 `scripts/preprocess_image.py` 的 `whitebg` 模式，抠图换白底并处理成 1024x1024 正方形居中。
3. 检查处理后的图片是否干净（无UI元素、衣服完整）；如仍有明确UI残留，再用PIL做局部白色遮罩。
4. `extract_white_panel.py` 和 `preprocess_image.py` 都必须以实际可用的 Python/CaaS 工具执行；不要声称“后台完成”而实际没有运行。

**单张图片工作流程（用户只发一张图时）：**

**自动判断处理模式：**
- **whitebg 模式（换白底）**：适用于衣服、裤子、鞋子、帽子、领带等单品平铺图/产品图。操作：抠图去除原背景 → 换成纯白色背景 → 裁剪成正方形 → 产品居中 → 统一尺寸 1024x1024。
- **square 模式（仅正方形）**：适用于项链佩戴图、真人穿着图、场景图等不适合换白底的图片。操作：保留原图内容 → 按短边做中心正方形裁剪 → 统一尺寸 1024x1024。此模式不进行“最大主体”检测，避免误裁真人和复杂背景。

**判断规则：**
- 看到单品平铺/产品图（背景简单、主体是服饰单品）→ whitebg
- 看到真人佩戴/穿着、场景图、复杂背景（如项链戴在脖子上、模特穿着图）→ square
- 不确定时优先用 square，保留原图信息

**执行命令：**
```bash
python3 scripts/preprocess_image.py <输入路径> <输出路径> <whitebg|square> 1024
```
> **注意**：whitebg 优先使用 `rembg`。若 `rembg` 不可用或运行失败，脚本使用保守的近白背景主体检测；若仍无法可靠识别主体，则保留原图并明确提示需人工检查，绝不把中心裁剪冒充为“已抠图”。

处理后的图片会放入后续的关键词复制框HTML页面，在产品字段下方显示小尺寸预览（最大180px），并点击后弹出大图查看器，用户可长按保存到相册（兼容iOS）。

### 后台步骤2：识别单品，提取品牌/品名/产品（不单独展示）

用户上传图片或描述单品时，先在后台完成以下识别，**不在输出中单独展示**：
- 读取图片，确认单品的品类、颜色、版型、关键细节
- 如果用户同时发送了商品标题，从中提取三个字段：
  - **品牌**：标题最前面的品牌名（如「MUKHINA」「yemanup」）
  - **产品**：品牌名之后的完整商品描述
  - **品名**：从产品描述中提炼的简洁名称，保留核心设计+品类，尽量简短（建议≤12字）
- **如果用户只发图片没有标题（默认情况），从图片中自动识别**：
  - **品牌**：通过图片中的logo、刺绣、领标、吊牌、印花等识别品牌名；仔细查看领口、胸口、袖口、下摆等位置的标识；如果品牌无法明确识别，标注「未识别」
  - **产品**：根据衣服的款式、颜色、设计元素、版型、工艺等自动生成产品描述，如「蓝白撞色条纹连帽拉链针织衫宽松毛衣外套」
  - **品名**：从产品描述中提炼简洁名称，保留核心设计+品类，尽量简短（建议≤12字），如「撞色条纹连帽针织衫」
- 这三个字段会放入后续的关键词复制框HTML页面，各自带独立拷贝按钮

### 第一步：五部分深度解析（最先呈现，核心输出）

后台识别完成后，**直接输出**以下五个部分，每部分有固定的子结构和写作要求。**这是完整解析的标准格式，每次必须严格遵循，且必须最先呈现。**

#### 一、历史溯源

写清楚这件单品的来龙去脉：
1. **起源**：时间、地点、最初用途、面料特点（如「19世纪美国西部，矿工牛仔的劳动工装，粗厚丹宁耐磨抗造」）
2. **演变**：什么时候走出原用途，成为文化符号，融入了哪些穿搭体系（如「20世纪50年代走出工地，成为青年文化符号，后续融入Ivy校园、复古工装」）
3. **本款定位**：当前这款的现代改良特点（如「现代改良直筒版型，中度水洗，兼顾工装基因与都市日常」）
4. **服饰谱系**：一句话概括演变链条，点明本源（如「美国西部矿工工装丹宁裤 → 青年文化标志性单品 → 都市复古日常下装，本源为体力劳动工装」）

#### 二、时尚史脉络

按年代分段梳理，每段说清楚当时的风格定位和搭配场景：
- 起源年代：最初的功能定位和使用人群
- 关键转折年代：如何被亚文化/校园/时尚界接纳，成为什么符号
- 当代：当前的风格定位，适配哪些穿搭体系

例：19世纪工装 → 50-70年代反叛青年/校园标配 → 现代万能基础款

#### 三、色彩分析

1. **主色+辅料色**：主体颜色、五金颜色、明线/装饰色
2. **色彩属性**：冷调/暖调、饱和度、色系归属
3. **搭配兼容性**：和哪些颜色/单品可以兼容，能平衡什么
4. **点缀建议**：可以用什么小配饰作为色彩点缀

#### 四、制作与面料工艺

分点写清楚材质、版型、结构、细节：
1. **面料**：材质成分、水洗/处理程度、颜色调、肌理特征、手感软硬、舒适度
2. **版型**：腰位/领型/肩线、比例效果、适配的穿法（如扎进裤腰、敞开叠穿）
3. **结构细节**：口袋结构、五金件（纽扣/拉链）、明线颜色、织唛/标签、装饰元素
4. **裤管/袖口/下摆**：廓形走向、收口方式、适配鞋款或内搭

#### 五、地区文化溯源

分点写清楚地域归属：
1. **版型本源**：这个版型起源于哪里
2. **工艺/结构本源**：关键工艺或结构来自哪个地区的服饰工业体系
3. **改良地区**：现代改良版是哪里的剪裁，适配什么场景和人群

### 第二步：七维关键词 + 复制框HTML（每次必做）

完成深度解析后，必须从七个维度各提取 10 个关键词，生成带独立拷贝按钮的 HTML 页面，和完整解析一起交付。

- **七个维度**：风格定调、色彩构析、形制匠艺、本源溯往、衣脉沿革、风尚流变、地域文脉
- **提取标准**：见 [references/seven-dimensions.md](references/seven-dimensions.md)，每维目标 10 词，2-6 字为主，不重复，优先具体名词；如果可靠信息不足，不得为了凑满10词编造历史、文化、品牌或工艺事实，可减少数量或使用明确的中性视觉描述。
- **HTML 模板**：使用 [assets/keyword-copy-template.html](assets/keyword-copy-template.html)，替换占位符 `{{品牌}}`、`{{品名}}`、`{{产品}}`、`{{洗护建议}}`、`{{图片路径}}` 和各维度关键词数组
- **页面顶部字段**（固定顺序）：
  - **品牌**：从商品标题提取的品牌名（如 MUJI），纯显示，**无拷贝按钮**
  - **品名**：提炼的简洁单品名（保留核心设计+品类，建议≤12字，如 基础宽松白色长袖衬衫），带拷贝按钮
  - **单品名称**：品牌+产品完整描述合并（如 MUJI 男式水洗平纹长袖衬衫 男装衬衣外套冬季纯棉全棉），放在品名下方，带拷贝按钮
- **洗护建议**（自动判断，固定放在单品名称下方、图片上方）：根据面料成分自动生成洗护建议（机洗水温/可否干洗/可否熨烫/反面洗涤/悬挂晾干等）；有内容则渲染模块+拷贝按钮，无内容则整段隐藏不输出。占位符 `{{洗护建议}}`
- **产品图片预览**：在洗护建议下方显示处理后的单品图片，小尺寸预览（最大180px），图片可直接点击弹出大图查看器，**长按可拷贝图片**（兼容iOS）。**必须将图片转base64嵌入HTML**（`{{图片路径}}` 占位符填入 `data:image/jpeg;base64,xxx`），确保HTML自包含、离线可独立打开，禁止使用外部URL
- **文件命名规则**：HTML文件名使用「品牌名_品名」格式，简洁易查找。将空格、`*`、`/`等特殊字符替换为下划线`_`。例如用户发「Basicannals复古撞色条纹连帽拉链针织衫男女宽松毛衣外套短宽」，品牌为Basicannals，品名为「撞色条纹连帽拉链针织衫」，文件名为 `Basicannals_撞色条纹连帽拉链针织衫.html`
- **交付方式**：生成真实 HTML 文件到当前可写工作目录（通常为 `/mnt/data/`），然后通过当前环境支持的文件附件/下载链接交付。不要依赖不存在的 `present_files` 工具。

## 参考资料

- [references/point-dimensions.md](references/point-dimensions.md)：「点」拆解的八大维度清单（思考时用）
- [references/association-patterns.md](references/association-patterns.md)：「线」的七种关联模式（思考时用）
- [references/seven-dimensions.md](references/seven-dimensions.md)：七维关键词提取标准

## 交互原则

- 用户上传图片时，先在后台完成图片预处理（换白底/正方形居中）和品牌/品名/产品提取，**不在输出中展示过程**
- 完整解析必须严格按五部分结构输出，且**必须最先呈现**，不要省略任何一部分
- 每次分析必须同时产出：① 五部分深度解析（最先呈现）；② 七维关键词复制框HTML（含品牌/品名/产品三字段+产品图片小预览，均可独立拷贝/点击图片查看大图保存）。两部分一起交付，不用等用户额外要求
- 遇到不确定的历史/文化事实，标注「待查证」，不要编造。将“图片可直接观察到的事实”“基于视觉的合理判断”“有来源支持的历史事实”分开；视觉判断不得伪装成历史事实。
- 品牌识别采用保守原则：只有图片文字/Logo/标题足以明确确认时才直接写品牌；无法确认时写「未识别」或「疑似××」，不要凭相似Logo猜测。
- 产品成分、洗护、产地等无法从图片或用户提供的商品信息确认时，不要杜撰；可以写“成分未提供，无法确认”。
- 整个过程用中文，输出结构清晰，方便用户存档积累

## 工具与可靠性约束

- 可用脚本：`scripts/extract_white_panel.py`、`scripts/preprocess_image.py`、`scripts/generate_html.py`。
- `generate_html.py` 负责 HTML 自包含化：产品图片自动转 base64 Data URI，关键词使用 JSON 序列化，避免引号、换行、HTML/JavaScript 特殊字符破坏页面。
- HTML 模板不得依赖外部图片、CDN、字体或网络接口，确保离线打开仍可用。
- `square` 模式只做中心正方形裁剪；不要使用白底主体检测裁切真人/佩戴/场景图片。
- 图片预处理脚本失败时必须明确暴露失败状态，不能把未抠图图片描述为“已完成抠图”。
- 如果当前运行环境没有 shell/Python 执行能力，不要虚构脚本已经执行；应直接使用可用能力生成结果，或说明无法完成文件生成。
